# Evidence

Commit only compact evidence that can be reviewed in git:

- JSON summaries;
- CSV tables;
- Markdown notes;
- SHA-256 manifests;
- small text logs.

Do not commit raw EBSD datasets or large numerical arrays. Preserve those
externally and record hashes plus provenance.
